
function hoanv12(){
	$('.show-details-btn').on('click', function() {
		$(this).closest('tr').next().toggleClass('open');
		$(this).find(ace.vars['.icon']).toggleClass('fa-angle-double-down').toggleClass('fa-angle-double-up');
	});
}

function deleteData(id){
	var ib = confirm('Bạn chắc chắn muốn xóa');
	if(ib){
		window.location.replace("http://127.0.0.1:8000/admin/product/delete/"+id);
	}
}

function UpdateData(id){
	window.location.replace("http://127.0.0.1:8000/admin/product/update/"+id);
}